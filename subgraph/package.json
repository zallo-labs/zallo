{
  "name": "subgraph",
  "version": "0.1",
  "scripts": {
    "generate": "(cd .. && yarn workspace contracts build) && graph codegen",
    "build:testnet": "mustache networks/testnet.json subgraph.mustache.yaml > subgraph.yaml && yarn graph build",
    "clean": "npx rimraf build data generated subgraph.yaml",
    "auth": "graph auth --product hosted-service",
    "deploy": "graph deploy --product hosted-service"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.40.0",
    "@graphprotocol/graph-ts": "0.29.3"
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  }
}
