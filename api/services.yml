version: '3.8'

services:
  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - zallo-api-redis:/data

volumes:
  zallo-api-redis:
    driver: local
