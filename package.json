{
  "name": "metasafe",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "version": "0.1.0",
  "author": "Hayden Briese <haydenbriese@gmail.com>",
  "workspaces": [
    "api",
    "app",
    "contracts",
    "lib",
    "subgraph"
  ],
  "scripts": {
    "api": "yarn workspace api",
    "app": "yarn workspace app",
    "contracts": "yarn workspace contracts",
    "lib": "yarn workspace lib",
    "subgraph": "yarn workspace subgraph",
    "lint": "eslint \"**/*.{j,t}s\"",
    "lint:fix": "yarn lint --fix",
    "clean": "yarn workspaces foreach run clean"
  },
  "engines": {
    "node": "16.x",
    "yarn": "3.2.0"
  },
  "//": "Add jest to avoid no app jest no such file or directory error",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.15.0",
    "@typescript-eslint/parser": "^5.15.0",
    "eslint": "^8.11.0",
    "jest": "^26.6.3"
  }
}
