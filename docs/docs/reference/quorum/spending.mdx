import TokenLimitInputSchema from '../schema/inputs/token-limit-input.mdx';

# Spending limits

Set spending limits on a per-token basis, limiting the amount spent in a given period (day, week or month).

## Fallback

You may either `allow` or `deny` spending of tokens without a limit.

Fallback is `allow` by default.

## TokenLimit Schema

<TokenLimitInputSchema />
