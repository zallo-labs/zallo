import { SubscribeExample } from './examples.tsx';
import Schema from '../schema/subscriptions/proposal.mdx';

# Subscribing to Proposals

:::tip
Subscriptions are more efficient than querying repeatedly (polling).
:::

Subscribe to receive proposal updates (creation, approval or rejection) pushed from the API.

Only members of the proposal policy can subscribe.

## Example

<SubscribeExample />

---

## Schema

<Schema />
