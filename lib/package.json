{
  "name": "lib",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "yarn generate && tsc --build",
    "generate": "(cd .. && yarn config-ws build && yarn contracts typechain)",
    "clean": "npx rimraf dist",
    "start": "yarn build --watch"
  },
  "dependencies": {
    "@ethersproject/bignumber": "^5.6.0",
    "ethers": "^5.6.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "typescript": "^4.6.2"
  }
}
