{
  "name": "config",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "tsc --build",
    "clean": "npx rimraf dist",
    "start": "yarn build --watch"
  },
  "dependencies": {
    "dotenv": "^16.0.2"
  },
  "devDependencies": {
    "typescript": "^4.8.2"
  }
}
