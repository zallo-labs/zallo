name: Deployment

on:
  push:
    branches: [main]

concurrency:
  group: deployment-${{ github.ref }}
  cancel-in-progress: true

jobs:
  api:
    environment: main - api
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Deploy
        uses: fjogeleit/http-request-action@v1
        with:
          url: ${{ secrets.DEPLOY_HOOK }}
          method: POST

  app:
    environment: main - app
    runs-on: ubuntu-latest

    env:
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}

    steps:
      - name: 🚧 Checkout
        uses: actions/checkout@v3

      # Disabled until expo-cli node 18 support is sanctioned - https://github.com/expo/expo-cli/issues/4478
      # - name: 🚧 Setup Node
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: 18
      #     cache: 'yarn'

      # - name: 📦 Install
      #   run: yarn install

      # - name: 🚧 Setup Expo
      #   uses: expo/expo-github-action@v7
      #   with:
      #     token: ${{ secrets.EXPO_TOKEN }}
      #     expo-version: latest
      #     eas-version: latest

      # - name: 🏗️ Prebuild
      #   run: yarn app prebuild

      # - name: 🚀 Publish update
      #   run: yarn app eas update --auto --non-interactive

      # - name: 🏗️ Build
      #   run: yarn eas build -p android --profile main

      # - name: 🚀 Publish
      #   run: yarn eas submit -p android --profile main --latest

  subgraph:
    environment: main - subgraph
    runs-on: ubuntu-latest

    steps:
      - name: 🚧 Checkout
        uses: actions/checkout@v3

      - name: 🚧 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'yarn'

      - name: 📦 Install
        run: yarn install

      - name: 🚧 Configure
        run: yarn subgraph configure:testnet

      - name: 🚧 Auth
        run: yarn subgraph auth ${{ secrets.THEGRAPH_ACCESS_TOKEN }}

      - name: 🏗️ Generate
        run: yarn subgraph generate

      - name: 🏗️ Build
        run: yarn subgraph build

      - name: 🚀 Deploy
        run: yarn subgraph deploy $SUBGRAPH_ID

  # Docs deployment is handled by Netlify
